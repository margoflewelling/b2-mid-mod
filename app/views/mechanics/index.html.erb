  <h1>All Mechanics</h1>
  <% @mechanics.each do |mechanic| %>
    <section id = <%="#{mechanic.id}" %>>
      <p><%="#{mechanic.name} - #{mechanic.years_experience} years of experience"%></p>
  <% end %>
